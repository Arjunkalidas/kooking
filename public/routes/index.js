var express = require('express');

var profilerController = require('../controller/profileController');
var catalogController = require('../controller/catalogController');

module.exports.controller = function(app) {

};