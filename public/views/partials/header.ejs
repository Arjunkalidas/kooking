<!DOCTYPE html>
<!-- views/partials/header.ejs -->
<div id="holder">
    <div id="logo_holder">
        <a href="/index">
            <img class="logo_img" src="/assets/images/logo.png" alt="a red square with letters">
        </a>
    </div>

    <!-- The brand name and title phrase here -->
    <div class="title">
        <h1 id="banner_title">kooKING</h1>
        <p>Eat like a king, all day long</p>
    </div>
    <div id="user-icon-holder">
        <form method="GET">
        <div class="fa fa-user-circle fa-2x" id="user-icon">
            <% if(!isLogin) { %>
                <button type="submit" name="signIn" id="sign_in" formaction="/userProfile/myItems">Sign In</button>
                <%  } else { %>
                <button type="submit" name="signIn" id="sign_in" formaction="/userProfile/myItems">Welcome!</button>
            <% } %>
        </div>
        </form>
    </div>
</div>
